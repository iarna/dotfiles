if which svn > /dev/null; then
	function svn () {
		CMD=$1
		if [ -x "~/svn-bin/svn-$CMD" ]; then
			shift
			"~/svn-bin/svn-$CMD" "$@"
		elif which "svn-$CMD" > /dev/null; then
			shift
			"svn-$CMD" "$@"
		else
			command svn "$@"
		fi
	}
fi
